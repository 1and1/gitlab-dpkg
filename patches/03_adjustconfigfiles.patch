Description: adjusted config files to use correct directories
 adjusted config files to use correct directories by packagelayout etc.
Author: Andreas Wirooks <andreas.wirooks@1und1.de>

--- gitlab-6.2.4.orig/gitlab-shell/config.yml.example
+++ gitlab-6.2.4/gitlab-shell/config.yml.example
@@ -15,10 +15,10 @@ http_settings:
 # Give the canonicalized absolute pathname,
 # REPOS_PATH MUST NOT CONTAIN ANY SYMLINK!!!
 # Check twice that none of the components is a symlink, including "/home".
-repos_path: "/home/git/repositories"
+repos_path: "/var/lib/gitlab/repositories"
 
 # File used as authorized_keys for gitlab user
-auth_file: "/home/git/.ssh/authorized_keys"
+auth_file: "/var/lib/gitlab/.ssh/authorized_keys"
 
 # Redis settings used for pushing commit notices to gitlab
 redis:
@@ -30,7 +30,7 @@ redis:
 
 # Log file.
 # Default is gitlab-shell.log in the root directory.
-# log_file: "/home/git/gitlab-shell/gitlab-shell.log"
+log_file: "/var/log/gitlab/gitlab-shell.log"
 
 # Log level. INFO by default
 log_level: INFO
--- gitlab-6.2.4.orig/config/gitlab.yml.example
+++ gitlab-6.2.4/config/gitlab.yml.example
@@ -29,7 +29,7 @@ production: &base
     # relative_url_root: /gitlab
 
     # Uncomment and customize if you can't use the default user to run GitLab (default: 'git')
-    # user: git
+    user: git
 
     ## Email settings
     # Email address used in the "From" field in mails sent by GitLab
@@ -50,9 +50,8 @@ production: &base
     ##   COLOR  = 5
     # default_theme: 2 # default: 2
 
-
     ## Users management
-    # default: false - Account passwords are not sent via the email if signup is enabled. 
+    # default: false - Account passwords are not sent via the email if signup is enabled.
     # signup_enabled: true
 
     ## Automatic issue closing
@@ -157,7 +156,7 @@ production: &base
   # GitLab Satellites
   satellites:
     # Relative paths are relative to Rails.root (default: tmp/repo_satellites/)
-    path: /home/git/gitlab-satellites/
+    path: /var/lib/gitlab/gitlab-satellites/
 
   ## Backup settings
   backup:
@@ -167,8 +166,8 @@ production: &base
   ## GitLab Shell settings
   gitlab_shell:
     # REPOS_PATH MUST NOT BE A SYMLINK!!!
-    repos_path: /home/git/repositories/
-    hooks_path: /home/git/gitlab-shell/hooks/
+    repos_path: /var/lib/gitlab/repositories/
+    hooks_path: /var/lib/gitlab/gitlab-shell/hooks/
 
     # Git over HTTP
     upload_pack: true
--- gitlab-6.2.4.orig/config/unicorn.rb.example
+++ gitlab-6.2.4/config/unicorn.rb.example
@@ -30,24 +30,24 @@ worker_processes 2
 
 # Help ensure your application will always spawn in the symlinked
 # "current" directory that Capistrano sets up.
-working_directory "/home/git/gitlab" # available in 0.94.0+
+working_directory "/usr/share/gitlab" # available in 0.94.0+
 
 # listen on both a Unix domain socket and a TCP port,
 # we use a shorter backlog for quicker failover when busy
-listen "/home/git/gitlab/tmp/sockets/gitlab.socket", :backlog => 64
+listen "/var/run/gitlab/gitlab.socket", :backlog => 64
 listen "127.0.0.1:8080", :tcp_nopush => true
 
 # nuke workers after 30 seconds instead of 60 seconds (the default)
 timeout 30
 
 # feel free to point this anywhere accessible on the filesystem
-pid "/home/git/gitlab/tmp/pids/unicorn.pid"
+pid "/var/run/gitlab/unicorn.pid"
 
 # By default, the Unicorn logger will write to stderr.
 # Additionally, some applications/frameworks log to stderr or stdout,
 # so prevent them from going to /dev/null when daemonized here:
-stderr_path "/home/git/gitlab/log/unicorn.stderr.log"
-stdout_path "/home/git/gitlab/log/unicorn.stdout.log"
+stderr_path "/var/log/gitlab/unicorn.stderr.log"
+stdout_path "/var/log/gitlab/unicorn.stdout.log"
 
 # combine Ruby 2.0.0dev or REE with "preload_app true" for memory savings
 # http://rubyenterpriseedition.com/faq.html#adapt_apps_for_cow
--- gitlab-6.2.4.orig/config/database.yml.postgresql
+++ gitlab-6.2.4/config/database.yml.postgresql
@@ -4,11 +4,11 @@
 production:
   adapter: postgresql
   encoding: unicode
-  database: gitlabhq_production
+  database: gitlab
   pool: 10
-  # username: git
-  # password:
-  # host: localhost
+  username: gitlab
+  password: 'gitlab'
+  host: localhost
   # port: 5432 
   # socket: /tmp/postgresql.sock
 
@@ -18,10 +18,11 @@ production:
 development:
   adapter: postgresql
   encoding: unicode
-  database: gitlabhq_development
+  database: gitlab
   pool: 5
-  username: postgres
-  password:
+  username: gitlab
+  password: 'gitlab'
+  host: localhost
   # socket: /tmp/postgresql.sock
 
 #
@@ -30,10 +31,11 @@ development:
 staging:
   adapter: postgresql
   encoding: unicode
-  database: gitlabhq_staging
+  database: gitlab
   pool: 5
-  username: postgres
-  password:
+  username: gitlab
+  password: 'gitlab'
+  host: localhost
   # socket: /tmp/postgresql.sock
 
 # Warning: The database defined as "test" will be erased and
@@ -42,8 +44,9 @@ staging:
 test: &test
   adapter: postgresql
   encoding: unicode
-  database: gitlabhq_test
+  database: gitlab
   pool: 5
-  username: postgres
-  password:
+  username: gitlab
+  password: 'gitlab'
+  host: localhost
   # socket: /tmp/postgresql.sock
--- gitlab-6.2.4.orig/config/database.yml.mysql
+++ gitlab-6.2.4/config/database.yml.mysql
@@ -5,11 +5,11 @@ production:
   adapter: mysql2
   encoding: utf8
   reconnect: false
-  database: gitlabhq_production
+  database: gitlab
   pool: 10
   username: gitlab
-  password: "secure password"
-  # host: localhost
+  password: 'gitlab'
+  host: localhost
   # socket: /tmp/mysql.sock
 
 #
@@ -19,10 +19,11 @@ development:
   adapter: mysql2
   encoding: utf8
   reconnect: false
-  database: gitlabhq_development
+  database: gitlab
   pool: 5
-  username: root
-  password: "secure password"
+  username: gitlab
+  password: 'gitlab'
+  host: localhost
   # socket: /tmp/mysql.sock
 
 # Warning: The database defined as "test" will be erased and
@@ -32,8 +33,9 @@ test: &test
   adapter: mysql2
   encoding: utf8
   reconnect: false
-  database: gitlabhq_test
+  database: gitlab
   pool: 5
-  username: root
-  password:
+  username: gitlab
+  password: 'gitlab'
+  host: localhost
   # socket: /tmp/mysql.sock
--- gitlab-6.2.4.orig/config/resque.yml.example
+++ gitlab-6.2.4/config/resque.yml.example
@@ -1,3 +1,3 @@
 development: redis://localhost:6379
 test: redis://localhost:6379
-production: redis://redis.example.com:6379
+production: redis://localhost:6379
